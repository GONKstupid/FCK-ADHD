import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gonkstupid.fckadhd',
  appName: 'FCK-ADHD',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
  },
};

export default config;
